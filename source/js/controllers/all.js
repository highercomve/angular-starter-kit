import angular from 'angular'

let controllers = angular.module('App.controllers', [])

// controllers.run(() => {
//   console.log('Running Controllers module')
// })

// How to add controller to the controllers module
// controllers.controller('ExampleController', require('./example-controller'))

export default controllers;
