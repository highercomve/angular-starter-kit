import angular from 'angular'

let directives = angular.module('App.directives', [])

// directives.run(() => {
//   console.log('Running directives module')
// })

// How to add controller to the controllers module
// directives.directive('ExampleDirective', require('./example-directive'))

export default directives
